# Retinal-Segmentation---UNet---PyTorch
A PyTorch U-Net model built from scratch to segment blood vessels in retinal scans. Features a custom training loop with Keras-style callbacks (Early Stopping, Checkpoints) and an ablation study demonstrating how removing standard ImageNet normalization improves Dice scores
